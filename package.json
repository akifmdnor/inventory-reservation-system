{
  "name": "inventory-reservation-system",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first inventory reservation with PostgreSQL, Redis guard, and premium ecommerce UI",
  "workspaces": ["web-client", "server"],
  "scripts": {
    "install:all": "npm install",
    "db:up": "docker compose -f server/docker-compose.yml up -d redis",
    "db:down": "docker compose -f server/docker-compose.yml down",
    "docker:up": "docker compose -f server/docker-compose.yml up -d --build && docker compose -f web-client/docker-compose.yml up -d --build",
    "docker:down": "docker compose -f web-client/docker-compose.yml down && docker compose -f server/docker-compose.yml down",
    "dev": "concurrently -n web,server -c cyan,magenta \"npm run dev -w web-client\" \"npm run dev -w server\"",
    "build": "npm run build -w server && npm run build -w web-client",
    "render:build:api": "npm run build -w server",
    "render:start:api": "npm run start -w server",
    "render:migrate:api": "npm run db:migrate -w server",
    "render:build:web": "npm run build -w web-client",
    "render:start:web": "npm run start -w web-client",
    "test": "npm run test -w server",
    "test:integration": "npm run test:integration -w server",
    "test:watch": "npm run test:watch -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
