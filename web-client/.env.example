# Dev: leave empty — Vite proxies /api → http://127.0.0.1:3002 (see vite.config.ts).
# Production same-origin: leave empty when UI and API share one host.
# Direct to API (optional): http://localhost:3002
# Never use port 3001 here unless you intend to hit spaceship-prms (requires JWT).
VITE_API_BASE=
